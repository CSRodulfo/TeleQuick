using System;
using DataAccess.Tests.Fixture;

namespace DataAccess.Tests
{
    public static class DataInitializer
    {
        public static void Initialize(IServiceProvider serviceProvider)
        {
            //ClientFixture.Initialize(serviceProvider);
        }
    }
}